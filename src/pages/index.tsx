import Head from "next/head";
import { api } from "~/utils/api";
import { SignInButton, SignOutButton, useUser } from "@clerk/nextjs";
import GamesPage from "./games/create";
import { useRouter } from "next/router";
import NavBar from "~/component/navbar";

export default function Home() {
  const { data } = api.games.getAll.useQuery();
  const user = useUser();
  const router = useRouter();

  return (
    <>
      <Head>
        <title>PlayScripe - keeping track of your gaming backlog</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="h-screen flex items-center justify-center backdrop-blur-lg">
        
        <div className="flex w-full max-w-4xl flex-col rounded-lg bg-[#41434A] drop-shadow-2xl md:flex-row">
          <div className="rounded-1-lg bg-gray-700 px-8 py-12 md:w-1/2">
            <h1 className="mb-4 text-center text-xl font-bold text-white">
              Join PlayScripe
            </h1>
            <h1 className="font-style: text-gray-300-mb-8 mb-4 text-center text-xs italic">
              Your Life in Games
            </h1>
            <form className="space-y-6">
              <div className="">
                <label className="block text-white">Email Address</label>
                <input className="mt-1 block border-0 border-b-2 border-gray-300 bg-transparent focus:border-blue-500 focus:ring-0 sm:text-sm"></input>
              </div>

              <div className="">
                <label className="block text-white">Password</label>
                <input className="mt-1 block border-0 border-b-2 border-gray-300 bg-transparent focus:border-blue-500 focus:ring-0 sm:text-sm"></input>
              </div>

              <div>
                <label>Remember Me</label>
                <input type="checkbox"></input>
              </div>

            </form>
            <div className="flex justify-center">
              {!user.isSignedIn && <SignInButton></SignInButton>}
              {!!user.isSignedIn && <button> Go to Creatwafwafwafwae</button>}
            </div>
          </div>
          <div className="md:w-1/2 px-8 py-12 flex items-center bg-LastOfUs2 bg-cover bg-center rounded-r-lg">
          
          </div>
        </div>
        <div></div>
      </main>
    </>
  );
}
